{
  "name": "docs",
  "type": "application",
  "source": "{projectRoot}",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn run quartz build",
        "cwd": "{projectRoot}"
      }
    }
  }
}
